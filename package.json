{
  "name": "autopulous-xsd",
  "description": "XML Schema Definition",
  "repository": "https://github.com/vertex/xsd",
  "license": "MIT",
  "private": false,
  "dependencies": {},
  "devDependencies": {
    "http-server": "^0.6.1",
    "jasmine-core": "^2.3.4",
    "karma": "~0.12",
    "karma-chrome-launcher": "^0.1.12",
    "karma-jasmine": "^0.3.5",
    "karma-junit-reporter": "^0.2.2",
    "karma-spec-reporter": "0.0.23",
    "shelljs": "^0.2.6"
  },
  "scripts": {
    "bower cache clean": "bower cache clean",
    "bower cache refresh": "bower install --force",
    "postinstall": "bower install",
    "pretest": "npm install",
    "unit test active": "karma start test/karma.conf.js",
    "unit test once": "karma start test/karma.conf.js --single-run"
  }
}
